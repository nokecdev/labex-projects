To search for specific files:
```
sudo find / -name "flag.*"
```

Search flag in environment variables:
```
grep -R "labex" /home 2>/dev/null
grep -R "FLAG" / 2>/dev/null
```


```
cat ~/.bashrc | grep -i flag
cat ~/.profile
cat ~/.zshrc
cat /etc/environment
cat /etc/profile
```
