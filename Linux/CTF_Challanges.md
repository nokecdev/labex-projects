## Find a secret somewhere in /var/log folder.
Keresés gyakori kulcsszavakra, mint secret, key...

```
sudo grep -Ri "flag" /var/log 2>/dev/null
sudo grep -Ri "secret" /var/log 2>/dev/null
sudo grep -Ri "key" /var/log 2>/dev/null
sudo grep -Ri "pass" /var/log 2>/dev/null
sudo grep -Ri "token" /var/log 2>/dev/null
sudo grep -Ri "ctf" /var/log 2>/dev/null
```

